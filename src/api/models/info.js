const Info = `
type Info {
    last_update: CustomDate
    genres: Int
    writers: Int
    publishers: Int
    artists: Int
    issues: Int
    comics: ComicsCount
  }`;

  module.exports = {
    Info
  }